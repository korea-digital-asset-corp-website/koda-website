# KODA 테크블로그 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스타일 가이드 검증을 겸해 KODA 디자인 시스템에 맞는 테크블로그(목록 + 상세)를 구현한다.

**Architecture:** 공지 패턴을 따른다 — `data/blogPosts.ts`(메타 배열) + `data/blogContentMap.tsx`(slug→본문 컴포넌트 맵) + 글별 TSX 본문. 목록은 문서형(그리드 목록) 트랙, 상세는 문서형(본문) 트랙. 본문 킷(`PostElements.tsx`)은 노션 블록과 1:1 대응해 노션→TSX 변환이 기계적으로 가능하게 한다.

**Tech Stack:** Next.js 15 App Router + React 19 + TypeScript + Tailwind CSS v4 + next-intl 4. **새 의존성 0.**

**Spec:** `docs/superpowers/specs/2026-07-30-tech-blog-design.md`

## Global Constraints

- `docs/design/style-guide.md`와 `CLAUDE.md`의 하드 규칙을 전부 지킨다. 특히: 컬러는 유틸 클래스만, `rounded-[4px]` 고정, 타이포 유틸만, secondary 금지, 새 색·새 라이브러리 금지.
- **내부 링크는 `@/i18n/navigation`의 `Link`를 쓴다.** `next/link` 금지 (locale 접두사 유실).
- 날짜는 `YYYY.MM.DD` 점 표기, 포맷팅은 `@/utils/dateFormat`의 `formatDate(dateStr, locale, 'full')`.
- 글 본문은 한국어 고정. UI 문자열은 `blog` 네임스페이스로 ko/en 동시 추가(**ko 먼저** — 타입 원천).
- `globals.css` 수정 금지. 기존 컴포넌트·페이지 수정 금지(sitemap.ts 라우트 추가만 예외).
- 작업 브랜치 `feat/tech-blog`. 태스크마다 lint 후 커밋.
- **구현 시작 시 `frontend-design` 스킬을 로드**해 시각 판단에 활용한다(특히 Task 4·5).

---

## File Structure

| 파일 | 책임 |
|---|---|
| `src/data/blogPosts.ts` (신규) | `BlogPost` 타입 + 메타 배열(최신순) + 조회 헬퍼 |
| `src/data/blogContentMap.tsx` (신규) | slug→본문 컴포넌트 맵 (`noticeContentMap` 패턴) |
| `src/components/blog/PostElements.tsx` (신규) | 본문 킷: CodeBlock·InlineCode·Figure·Blockquote·Callout |
| `src/components/blog/posts/MpcKeyManagement.tsx` 외 2 (신규) | 글 본문 3편 |
| `src/components/blog/FeaturedPostCard.tsx`·`PostCard.tsx`·`PostGrid.tsx` (신규) | 목록 UI |
| `src/components/blog/PostHeader.tsx`·`AuthorCard.tsx`·`RelatedPosts.tsx`·`BackToBlogButton.tsx` (신규) | 상세 UI |
| `src/components/blog/index.ts` (신규) | 배럴 |
| `src/app/[locale]/blog/page.tsx`·`blog/[slug]/page.tsx` (신규) | 라우트 |
| `messages/ko.json`·`en.json` (수정) | `blog` 네임스페이스 |
| `src/app/sitemap.ts` (수정) | `/blog` + 글 URL |
| `docs/design/blog-post-workflow.md` (신규) | 노션→TSX 등록 절차 |
| `docs/design/style-guide.md` 외 파생 2 (수정) | 검증 수확 반영 |

### Task 순서

```
Task 1 (데이터 + i18n) → Task 2 (본문 킷) → Task 3 (글 3편 + 맵)
  → Task 4 (목록) → Task 5 (상세) → Task 6 (sitemap·워크플로 문서·검증·가이드 반영)
```

---

### Task 1: 데이터 모델과 i18n 메시지

**Files:**
- Create: `src/data/blogPosts.ts`
- Modify: `messages/ko.json`, `messages/en.json` (최상위에 `blog` 키 추가)

**Interfaces:**
- Produces: `BlogCategory`(유니온), `BlogAuthor`, `BlogPost` 인터페이스, `blogPosts: BlogPost[]`(최신순), `getPostBySlug(slug: string): BlogPost | undefined`, `getRelatedPosts(slug: string, limit?: number): BlogPost[]`(같은 카테고리 우선, 자신 제외, 최신순).
- i18n 키: `blog.list.title`·`blog.list.description`·`blog.list.featuredLabel`·`blog.detail.relatedTitle`·`blog.detail.backToList`·`blog.meta.title`·`blog.meta.description`.

- [ ] **Step 1: `src/data/blogPosts.ts` 작성**

```ts
export type BlogCategory = 'Security' | 'Engineering' | 'Insight';

export interface BlogAuthor {
  name: string;
  role: string;
}

export interface BlogPost {
  slug: string;
  title: string;
  description: string;
  category: BlogCategory;
  date: string; // 'YYYY.MM.DD'
  author: BlogAuthor;
  thumbnail?: string;
}

// 최신순 정렬을 데이터가 보장한다 — 맨 앞이 최신이며 목록 히어로가 된다.
export const blogPosts: BlogPost[] = [
  {
    slug: 'cold-wallet-withdrawal-automation',
    title: '콜드월렛 출금 승인 파이프라인 자동화',
    description: '수탁 서비스의 생명선인 출금 승인 과정을 사람의 실수 없이, 그러나 사람의 통제 아래 자동화한 과정을 공유합니다.',
    category: 'Engineering',
    date: '2026.06.18',
    author: { name: '박온체인', role: 'Platform Engineer' },
  },
  {
    slug: 'mpc-key-management',
    title: 'MPC 기반 키 관리 아키텍처',
    description: '단일 실패 지점 없는 키 관리를 위해 KODA가 MPC를 도입하며 검토한 설계 원칙과 운영 교훈을 정리했습니다.',
    category: 'Security',
    date: '2026.04.02',
    author: { name: '김커스터디', role: 'Security Engineer' },
  },
  {
    slug: 'digital-asset-custody-trends',
    title: '디지털자산 수탁 시장의 세 가지 흐름',
    description: '기관 자금 유입, 규제 정비, 수탁 기술의 표준화 — 2026년 상반기 수탁 시장을 관통하는 흐름을 짚어봅니다.',
    category: 'Insight',
    date: '2026.02.11',
    author: { name: '이애널리스트', role: 'Research Lead' },
  },
];

export const getPostBySlug = (slug: string): BlogPost | undefined => {
  return blogPosts.find((post) => post.slug === slug);
};

export const getRelatedPosts = (slug: string, limit = 2): BlogPost[] => {
  const current = getPostBySlug(slug);
  if (!current) return [];
  const sameCategory = blogPosts.filter((post) => post.slug !== slug && post.category === current.category);
  const others = blogPosts.filter((post) => post.slug !== slug && post.category !== current.category);
  return [...sameCategory, ...others].slice(0, limit);
};
```

- [ ] **Step 2: 메시지 추가 — ko 먼저, en 동시**

`messages/ko.json` 최상위(예: `modal` 앞)에:

```json
"blog": {
  "list": {
    "title": "KODA 테크블로그",
    "description": "디지털자산 수탁을 만드는 기술과 고민을 기록합니다.",
    "featuredLabel": "최신 글"
  },
  "detail": {
    "relatedTitle": "함께 읽기",
    "backToList": "목록으로"
  },
  "meta": {
    "title": "테크블로그",
    "description": "KODA 기술 조직이 디지털자산 수탁을 만들며 쌓은 기술과 인사이트를 공유합니다."
  }
}
```

`messages/en.json` 같은 위치에:

```json
"blog": {
  "list": {
    "title": "KODA Tech Blog",
    "description": "Notes on the engineering behind digital asset custody.",
    "featuredLabel": "Latest"
  },
  "detail": {
    "relatedTitle": "Related posts",
    "backToList": "Back to list"
  },
  "meta": {
    "title": "Tech Blog",
    "description": "Engineering write-ups and insights from the team building KODA's digital asset custody."
  }
}
```

- [ ] **Step 3: 검증 — 타입 정합과 키 대칭**

```bash
npx tsc --noEmit 2>&1 | head -5
node -e "
const ko=require('./messages/ko.json'),en=require('./messages/en.json');
const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'&&!Array.isArray(v)?flat(v,p+k+'.'):[p+k]);
const a=new Set(flat(ko.blog||{})),b=new Set(flat(en.blog||{}));
console.log('blog 키:',a.size,'/ ko전용:',[...a].filter(k=>!b.has(k)),'/ en전용:',[...b].filter(k=>!a.has(k)));
"
```

Expected: tsc 오류 0, blog 키 7, ko전용·en전용 빈 배열.

- [ ] **Step 4: 커밋** — `feat(blog): 블로그 데이터 모델과 i18n 메시지 추가`

---

### Task 2: 본문 컴포넌트 킷

**Files:**
- Create: `src/components/blog/PostElements.tsx`

**Interfaces:**
- Produces (Task 3의 글 본문이 소비):
  - `CodeBlock({ language?: string; children: React.ReactNode })` — 언어 라벨 + `<pre><code>` 블록
  - `InlineCode({ children: React.ReactNode })`
  - `Figure({ src: string; alt: string; caption?: string; width: number; height: number })`
  - `Blockquote({ children: React.ReactNode })`
  - `Callout({ children: React.ReactNode })`
- 기존 `H2`·`H3`·`P`·`Ol`·`Ul`·`Li`(`@/components/typography`)와 함께 쓰는 것을 전제한다 — 재정의하지 않는다.

- [ ] **Step 1: `PostElements.tsx` 작성**

```tsx
import Image from 'next/image';

interface ChildrenProps {
  children: React.ReactNode;
}

interface CodeBlockProps extends ChildrenProps {
  language?: string;
}

// 노션 code 블록 대응. 신택스 하이라이팅 없음(의존성 0 원칙) — 면과 테두리로만 구분한다.
export function CodeBlock({ language, children }: CodeBlockProps) {
  return (
    <div className="my-6 border border-gray-50 rounded-[4px] overflow-hidden">
      {language && (
        <div className="px-4 py-2 border-b border-gray-50 text-caption-lg text-gray-500 font-medium">{language}</div>
      )}
      <pre className="bg-gray-5020 p-4 overflow-x-auto">
        <code className="font-mono text-body-sm text-gray-700 whitespace-pre">{children}</code>
      </pre>
    </div>
  );
}

// 노션 인라인 코드 대응.
export function InlineCode({ children }: ChildrenProps) {
  return <code className="bg-gray-5020 text-primary-800 font-mono text-body-sm rounded-[4px] px-1.5 py-0.5">{children}</code>;
}

interface FigureProps {
  src: string;
  alt: string;
  caption?: string;
  width: number;
  height: number;
}

// 노션 image 블록 대응.
export function Figure({ src, alt, caption, width, height }: FigureProps) {
  return (
    <figure className="my-8">
      <Image src={src} alt={alt} width={width} height={height} className="mx-auto rounded-[4px]" />
      {caption && (
        <figcaption className="mt-3 text-caption-lg text-gray-500 text-center">{caption}</figcaption>
      )}
    </figure>
  );
}

// 노션 quote 블록 대응.
export function Blockquote({ children }: ChildrenProps) {
  return (
    <blockquote className="my-6 border-l-2 border-primary-700 pl-5 text-body-sm lg:text-body-lg text-gray-700">
      {children}
    </blockquote>
  );
}

// 노션 callout 블록 대응.
export function Callout({ children }: ChildrenProps) {
  return <aside className="my-6 bg-primary-50 rounded-[4px] p-5 text-body-sm lg:text-body-lg text-gray-700">{children}</aside>;
}
```

- [ ] **Step 2: 검증** — `npx tsc --noEmit` 오류 0. 금지 패턴 grep(`var(--color-`, `#[0-9a-f]`, `rounded-md|rounded-lg`) 0건.

- [ ] **Step 3: 커밋** — `feat(blog): 본문 컴포넌트 킷 추가`

---

### Task 3: 글 본문 3편과 콘텐츠 맵

**Files:**
- Create: `src/components/blog/posts/MpcKeyManagement.tsx`, `ColdWalletWithdrawalAutomation.tsx`, `DigitalAssetCustodyTrends.tsx`
- Create: `src/data/blogContentMap.tsx`

**Interfaces:**
- Consumes: Task 2의 킷 전부 + `@/components/typography`의 `H2`·`H3`·`P`·`Ol`·`Ul`·`Li`.
- Produces: `getBlogContent(slug: string): React.ComponentType | null` (`noticeContentMap`의 `getNoticeContent` 패턴).

- [ ] **Step 1: 글 3편 작성**

각 글은 default export 함수 컴포넌트(서버 컴포넌트, `'use client'` 없음). 분량은 H2 섹션 3개 내외 + 섹션당 2~3문단. 킷 검증 배치가 핵심이다:

- **MpcKeyManagement** (Security): 섹션 "왜 단일 키는 위험한가" / "MPC 서명 흐름" / "운영에서 배운 것". `CodeBlock`(language 없이 — 임계값 서명 수도코드), `Blockquote`(NIST 인용 형식의 한 문장), `Callout`(키 셰어 보관 정책 요약) 각 1회.
- **ColdWalletWithdrawalAutomation** (Engineering): 섹션 "수동 승인의 병목" / "파이프라인 설계" / "장애 시나리오와 롤백". `CodeBlock`(`language="typescript"` — 승인 상태 머신 타입 정의 15줄 내외), `InlineCode` 3회 이상(상태 이름들), `Ol`(승인 단계 목록) 1회.
- **DigitalAssetCustodyTrends** (Insight): 섹션 "기관 자금의 유입" / "규제 정비" / "수탁 기술의 표준화". `Blockquote` 1회, `Ul` 1회, `Figure` 1회(`src="/assets/images/img_graph.png"`, width 482, height 482, 캡션 "수탁 자산 규모 추이(예시 이미지)").

본문 문장은 해당 주제의 실무 서술로 작성한다(로렘 입숨 금지). 수치·기관명은 일반론 수준으로 유지해 사실 주장으로 읽히지 않게 한다.

- [ ] **Step 2: `src/data/blogContentMap.tsx` 작성**

```tsx
import React from 'react';
import MpcKeyManagement from '@/components/blog/posts/MpcKeyManagement';
import ColdWalletWithdrawalAutomation from '@/components/blog/posts/ColdWalletWithdrawalAutomation';
import DigitalAssetCustodyTrends from '@/components/blog/posts/DigitalAssetCustodyTrends';

const blogContentMap: Record<string, React.ComponentType> = {
  'mpc-key-management': MpcKeyManagement,
  'cold-wallet-withdrawal-automation': ColdWalletWithdrawalAutomation,
  'digital-asset-custody-trends': DigitalAssetCustodyTrends,
};

export const getBlogContent = (slug: string): React.ComponentType | null => {
  return blogContentMap[slug] || null;
};
```

- [ ] **Step 3: 검증** — `npx tsc --noEmit` 오류 0. 맵 키 3개가 `blogPosts`의 slug 3개와 일치하는지 node 스크립트로 대조.

- [ ] **Step 4: 커밋** — `feat(blog): 더미 글 3편과 콘텐츠 맵 추가`

---

### Task 4: 목록 페이지

**Files:**
- Create: `src/components/blog/FeaturedPostCard.tsx`, `PostCard.tsx`, `PostGrid.tsx`, `index.ts`
- Create: `src/app/[locale]/blog/page.tsx`

**Interfaces:**
- Consumes: `blogPosts`, `BlogPost`(Task 1), i18n `blog.list.*`·`blog.meta.*`.
- Produces: `FeaturedPostCard({ post: BlogPost })`, `PostCard({ post: BlogPost })`, `PostGrid({ posts: BlogPost[] })`. 배럴 `index.ts`는 목록·상세 컴포넌트 전부 export(상세 컴포넌트는 Task 5에서 추가).

- [ ] **Step 1: 컴포넌트 작성**

전 컴포넌트 서버 컴포넌트. 날짜는 `formatDate(post.date, locale, 'full')`(`getLocale()` 사용). 링크는 **`@/i18n/navigation`의 `Link`**.

`FeaturedPostCard`: `<Link href={`/blog/${post.slug}`}>` 전체 래핑 + `group`. `grid lg:grid-cols-2` — 왼쪽 텍스트(카테고리 `text-label-sm font-semibold text-primary-700` → 제목 `text-headline-sm lg:text-headline-md font-bold group-hover:text-primary-700 transition-colors` → 요약 `text-body-md lg:text-body-lg text-gray-700 font-medium lg:font-normal` → 메타 `text-caption-lg text-gray-500`), 오른쪽 `bg-gray-5020 rounded-[4px]` 면에 카테고리 워드마크(`text-headline-md font-bold text-gray-300`, 중앙 정렬, `aspect-[3/2]`). 전체를 `border border-gray-50 rounded-[4px] p-6 lg:p-10`으로 감싼다.

`PostCard`: `<article className="group border border-gray-50 rounded-[4px] bg-white h-full">` 안에 `<Link className="flex flex-col h-full p-6 lg:p-8">` — 카테고리 라벨 → 제목(`text-headline-xs lg:text-headline-sm font-bold group-hover:text-primary-700 transition-colors`) → 요약(`text-body-sm lg:text-body-md text-gray-700`, `line-clamp-2`) → `mt-auto`로 바닥에 메타(날짜 · 저자명, `text-caption-lg text-gray-500`).

`PostGrid`: `<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">` + `posts.map`.

- [ ] **Step 2: `blog/page.tsx` 작성**

문서 목록 트랙 — 진입 애니메이션 없음. 컨테이너는 **그리드형 목록 규칙**: `max-w-[1440px] w-full mt-[72px] lg:mt-32 mb-20 lg:mb-[180px] px-5 lg:px-[100px] mx-auto`. 구조: h1(`text-headline-sm lg:text-headline-lg font-bold`) + 소개문 → `FeaturedPostCard post={blogPosts[0]}` → `PostGrid posts={blogPosts.slice(1)}`. `generateMetadata`는 `blog.meta.*`에서 생성(`alternates.languages` ko/en/x-default 포함).

- [ ] **Step 3: 검증** — lint·build 통과, 라우트 목록에 `/[locale]/blog` 존재. 금지 패턴 grep 0건.

- [ ] **Step 4: 커밋** — `feat(blog): 블로그 목록 페이지 추가`

---

### Task 5: 상세 페이지

**Files:**
- Create: `src/components/blog/PostHeader.tsx`, `AuthorCard.tsx`, `RelatedPosts.tsx`, `BackToBlogButton.tsx`
- Create: `src/app/[locale]/blog/[slug]/page.tsx`
- Modify: `src/components/blog/index.ts` (상세 컴포넌트 export 추가)

**Interfaces:**
- Consumes: `getPostBySlug`·`getRelatedPosts`(Task 1), `getBlogContent`(Task 3), `PostCard`(Task 4), i18n `blog.detail.*`.
- Produces: `PostHeader({ post: BlogPost })`, `AuthorCard({ author: BlogAuthor })`, `RelatedPosts({ posts: BlogPost[] })`, `BackToBlogButton()`.

- [ ] **Step 1: 컴포넌트 작성**

`PostHeader`: 카테고리 라벨(`text-label-sm font-semibold text-primary-700`) → h1(`text-headline-sm lg:text-headline-lg font-bold mt-2 mb-4 lg:mb-6`) → 메타 한 줄(날짜 · 저자명 · 역할, `text-caption-lg lg:text-body-md text-gray-500`) → `<hr className="border-gray-50 mt-6 lg:mt-10" />`.

`AuthorCard`: `border border-gray-50 rounded-[4px] p-5 lg:p-6` — 이름(`text-title-md lg:text-title-lg font-semibold`) + 역할(`text-body-sm text-gray-500`). 아바타 없음(가짜 이미지 금지).

`RelatedPosts`: 제목 `blog.detail.relatedTitle`(`text-headline-xs lg:text-headline-sm font-bold`) + `grid sm:grid-cols-2 gap-6`에 `PostCard` 재사용.

`BackToBlogButton`: `BackToListButton` 패턴을 표준 클래스로 — `@/i18n/navigation` `Link`, `href="/blog"`, 2차 버튼 클래스(`border border-primary-700 text-primary-800 hover:bg-primary-50 ... rounded-[4px]`), 라벨 `blog.detail.backToList`.

- [ ] **Step 2: `blog/[slug]/page.tsx` 작성**

```tsx
import { notFound } from 'next/navigation';
import { getTranslations } from 'next-intl/server';
import type { Metadata } from 'next';
import { AuthorCard, BackToBlogButton, PostHeader, RelatedPosts } from '@/components/blog';
import { blogPosts, getPostBySlug, getRelatedPosts } from '@/data/blogPosts';
import { getBlogContent } from '@/data/blogContentMap';

interface BlogPostPageProps {
  params: Promise<{ locale: string; slug: string }>;
}

export function generateStaticParams() {
  return blogPosts.map((post) => ({ slug: post.slug }));
}

export async function generateMetadata({ params }: BlogPostPageProps): Promise<Metadata> {
  const { locale, slug } = await params;
  const post = getPostBySlug(slug);
  if (!post) return {};
  return {
    title: post.title,
    description: post.description,
    alternates: {
      canonical: `https://kodax.com/${locale}/blog/${post.slug}`,
      languages: {
        ko: `https://kodax.com/blog/${post.slug}`,
        en: `https://kodax.com/en/blog/${post.slug}`,
        'x-default': `https://kodax.com/blog/${post.slug}`,
      },
    },
  };
}

const BlogPostPage = async ({ params }: BlogPostPageProps) => {
  const { slug } = await params;
  const post = getPostBySlug(slug);
  const ContentComponent = getBlogContent(slug);

  if (!post || !ContentComponent) {
    notFound();
  }

  const related = getRelatedPosts(slug);

  return (
    <div className="max-w-[1440px] w-full mt-[72px] lg:mt-32 mb-20 lg:mb-[240px] px-5 lg:px-[200px] mx-auto">
      <PostHeader post={post} />
      <main className="mt-8 lg:mt-12">
        <ContentComponent />
      </main>
      <div className="mt-12 lg:mt-16 space-y-10 lg:space-y-14">
        <AuthorCard author={post.author} />
        {related.length > 0 && <RelatedPosts posts={related} />}
        <BackToBlogButton />
      </div>
    </div>
  );
};

export default BlogPostPage;
```

컨테이너에 `whitespace-pre-line` 없음 — 의도된 변형(스펙 6절).

- [ ] **Step 3: 검증** — lint·build 통과, 라우트 목록에 `/[locale]/blog/[slug]` 존재. 금지 패턴 grep 0건.

- [ ] **Step 4: 커밋** — `feat(blog): 블로그 상세 페이지 추가`

---

### Task 6: sitemap · 워크플로 문서 · 종합 검증 · 가이드 반영

**Files:**
- Modify: `src/app/sitemap.ts` (`/blog` + 글 3편 URL)
- Create: `docs/design/blog-post-workflow.md`
- Modify: `docs/design/style-guide.md`, `docs/design/page-prompt.md`, `CLAUDE.md` (검증 수확 반영)

- [ ] **Step 1: sitemap에 라우트 추가** — `routes` 배열에 `{ path: '/blog', changeFrequency: 'weekly', priority: 0.7 }` + `blogPosts.map`으로 글 URL(`changeFrequency: 'monthly'`, `priority: 0.6`) 추가. `blogPosts` import.

- [ ] **Step 2: `blog-post-workflow.md` 작성** — 등록 절차: ① 노션에 초안(제목·카테고리·요약·본문. 코드/이미지/인용/콜아웃은 노션 기본 블록 사용) ② Claude에게 "이 노션 페이지를 블로그 글로 등록해줘" — `notion_page_read`로 읽어 `posts/<PascalCase>.tsx` 생성(블록→킷 대응표 포함), `blogPosts.ts` 메타 맨 앞 추가, `blogContentMap.tsx` 연결, sitemap은 자동 반영됨 ③ `npm run lint`·`npm run build` ④ PR. 블록→컴포넌트 대응표(스펙 7절)를 그대로 싣는다.

- [ ] **Step 3: 종합 검증**

```bash
npm run lint && npm run build 2>&1 | grep -E 'blog|✓|Failed'
# 기계 검사: 금지 패턴 0건
grep -rn 'var(--color-\|text-\[#\|bg-\[#\|rounded-md\|rounded-lg\|from next/link' src/components/blog "src/app/[locale]/blog" src/data/blogPosts.ts src/data/blogContentMap.tsx || echo 없음
# ko/en 키 대칭 재확인 + dev 서버 육안 확인(목록·상세·404·en 로케일)
```

- [ ] **Step 4: 가이드에 검증 수확 반영** — 최소 2건 확정: ① 8.1 내부 링크를 `@/i18n/navigation` `Link`로 정정(기존 `next/link` 안내는 잠재 버그로 명시, 파생 문서 2개도 동기화) ② 4.1에 "TSX 본문 상세 페이지는 `whitespace-pre-line` 제외" 변형 추가. 구현 중 발견되는 추가 수확도 함께 기재.

- [ ] **Step 5: 커밋** — `feat(blog): sitemap 연결·글 등록 워크플로 문서화·가이드 검증 수확 반영`

---

## Self-Review 결과

- **스펙 커버리지**: 3절 파일 구조→전 태스크, 4절 데이터 모델→Task 1·3, 5절 목록→Task 4, 6절 상세→Task 5, 7절 킷→Task 2, 8절 더미→Task 3, 9절 i18n·SEO→Task 1·4·5·6, 11절 검증→각 태스크 Step + Task 6. 누락 없음.
- **타입 일관성**: `BlogPost`·`getPostBySlug`·`getRelatedPosts`·`getBlogContent` 시그니처가 Task 1→3→4→5에서 동일. `PostCard`를 Task 5의 `RelatedPosts`가 재사용.
- **플레이스홀더**: 글 본문 문단은 주제·섹션 제목·킷 배치를 확정하고 산문만 구현 시 작성 — 콘텐츠 저작 지시로 명확함.
